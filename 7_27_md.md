# CM-MAE: Physics-Grounded Cross-Modal Masked Autoencoder for Camera–Channel Representation Learning in ISAC

## 进展汇报（2026-07-28）

---

## 一、研究背景与核心问题

在通感一体化（Integrated Sensing and Communication, ISAC）系统中，mmWave 波束管理面临严峻挑战：传统波束扫描开销随天线数线性增长，高速移动场景下波束对准困难。视觉传感器（RGB 相机）可提供场景几何与遮挡信息，但如何建立视觉像素与无线信道之间的跨模态对应关系是关键瓶颈。

**核心科学问题**：视觉（像素网格）与无线信道（复值频响）模态差异极大，缺乏大规模配对标注数据，如何自监督地学习物理可解释的跨模态对齐表示？

---

## 二、CM-MAE 框架总览

我们提出 **Physics-Grounded Cross-Modal Masked Autoencoder (CM-MAE)**，一个面向 mmWave 波束预测的物理引导跨模态自监督预训练框架。

### 2.1 框架核心优势（对比现有 SOTA）

| 维度 | 现有方法（Vision-only / Late Fusion） | **CM-MAE（本文）** |
|------|--------------------------------------|-------------------|
| **预训练范式** | 端到端监督训练，依赖大量标注 | ✅ **自监督掩码预训练**，四阶段课程学习，从无标注数据中学习 |
| **模态融合** | 后期特征拼接/注意力融合 | ✅ **物理前端 + Token级Cross-Attention**，像素级与传播路径簇级对齐 |
| **CSI处理** | 仅用64维beam power向量 | ✅ **3D Beamspace变换 + NMS聚类**，提取物理传播路径簇（方位×仰角×时延） |
| **跨模态对齐** | 无显式对齐机制 | ✅ **Sinkhorn最优传输（双Dustbin）**，学习路径簇与图像区域的软对应 |
| **时序建模** | 单帧预测 | ✅ **因果时序Transformer + Beam History**，预测当前+未来3帧波束 |
| **泛化能力** | 同场景训练测试，跨场景差 | ✅ **BS-relative GPS编码 + 多场景训练**，支持跨场景泛化 |
| **可解释性** | 黑盒分类器 | ✅ **物理传播路径可视化 + 跨模态注意力热图**，可解释波束选择依据 |

### 2.2 模型架构（ViT-Base 规模，~232M 参数）

```
图像 I (B,3,224,224)               mmWave 信道 H (B,16,8,128,complex)
       │                                      │
       ▼                                      ▼
  PatchEmbed (16×16)                 3D Beamspace Transform
  + Ray Direction Encoding           (oversampled DFT: az×el×τ)
       │                                      │
       ▼                                      ▼
  196 vision tokens                 Q propagation path clusters
  (B,196,768)                        (B,Q,768) + energy/quality emb
  + Fourier 3D PosEnc                 + Fourier 3D PosEnc (θ,φ,τ)
       │                                      │
       ▼                                      ▼
  ┌──────────────┐               ┌──────────────────┐
  │ Vision ViT   │◄── CLS token  │ CSI ViT Encoder  │◄── Scale Token
  │ Encoder      │               │ (12 layers)      │   (替代CLS，避免冗余)
  │ (12 layers,  │               │ d=768, h=12      │
  │  d=768, h=12)│               └────────┬─────────┘
  └──────┬───────┘                        │
         │                                │
         ▼                                ▼
    vis_cls (B,768)                 csi_scale (B,768)
    vis_tokens (B,196,768)          csi_tokens (B,Q,768)
         │                                │
         └────────────┬───────────────────┘
                      ▼
            ┌─────────────────────┐
            │  Joint Decoder      │  modality-type embeddings
            │  (8 Transformer     │  mask tokens + cross-attention
            │   layers)           │  双模态信息互补重建
            └──────────┬──────────┘
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
    Vision Rec   CSI Rec    Contrastive/OT
    (pixel MSE)  (beam MSE) (跨模态对齐)
```

### 2.3 Beam Tracking 下游架构（v3）

预训练编码器基础上，增加：

- **时序 Transformer**（2层，d=768）：聚合 T=5 历史帧 + K=4 未来帧特征
- **Beam History 因果特征**：前 T-1 帧的 64 维 power 向量作为输入，建模波束切换的因果关系
- **GPS Residual Head**（零初始化线性层）：BS-relative GPS 坐标编码，作为视觉预测的残差修正
- **GPS Projection**（零初始化）：将 GPS 特征注入时序序列，支持位置感知的波束预测
- **输出**：Seq2Seq 预测当前帧 + 未来 3 帧的 Top-K beam 索引

---

## 三、关键技术创新

### 3.1 物理可解释的信道前端

**3D Beamspace Transform**：将 16×8×128 的复值 CFR 通过过采样 DFT 字典（N_az=128, N_el=64, N_τ=128）变换到波束空间，每个能量峰值对应一条物理传播路径。

**NMS 连通域聚类**：3D 非极大值抑制提取局部极大值，连通域标记合并相邻能量，得到物理传播路径簇（azimuth × elevation × delay）。相比直接用 beam power 向量，保留了完整的多径空间-时延结构。

**Fourier 3D 位置编码**：用随机 Fourier 特征编码路径簇的 (θ, φ, τ) 三维连续坐标，比可学习位置编码更适合连续物理空间。

### 3.2 四阶段课程学习预训练

| 阶段 | 训练目标 | Mask策略 | 关键损失 | 目的 |
|------|---------|---------|---------|------|
| **Stage A** | 双模态重建 | Random masking (75% vis / 60% csi) | l_rec_v + l_rec_c (MSE) | 学习低层视觉/信道特征 |
| **Stage B1** | +全局对比对齐 | Random masking | + l_ctr (soft contrastive) | 跨模态全局语义对齐 |
| **Stage B2** | +几何引导掩码 | Geometry-guided masking | 重建 + 对比 | 学习遮挡/非视距场景的跨模态推理 |
| **Stage C** | +细粒度对应 | Geometry-guided masking | + l_ot (Sinkhorn OT) | 像素-路径簇级别的精确对应 |

**Soft Contrastive Loss**：利用 UE 位置高斯核和信道余弦相似度构造软标签，而非 binary positive/negative，物理先验引导对齐过程。

**Sinkhorn Dual Dustbin OT Loss**：最优传输框架学习视觉 patch 与 CSI 路径簇的软对应关系，双 dustbin 处理无匹配 token（背景像素、噪声簇），固定正样本质量分配避免梯度消失。

### 3.3 训练优化

- **向量化 OT Loss**：将 Sinkhorn 迭代完全向量化，单 iter 从 26s → **1.3s**，加速 20×
- **DDP 双卡训练**：2×A100 40G，batch_size=48×2=96，ViT-Base 模型
- **Layer-wise LR Decay**：Fine-tuning 时对预训练层使用更低学习率（0.02×），新层使用正常学习率
- **EMA（指数移动平均）**：稳定评估过程
- **Sequence-Consistent Augmentation**：时序帧使用相同增强参数，保持时序一致性

---

## 四、实验设置与数据集

### 4.1 DeepSense 6G 真实世界数据集

| 场景 | 描述 | 数据量 | 用途 |
|------|------|--------|------|
| Scenario 1-4 | 城市 V2I 日间场景（含夜间） | ~5000+ 样本 | 训练集 |
| Scenario 5 | 新场景（同城市不同位置） | ~800 样本 | 验证集 |
| Scenario 6-8 | 跨场景测试（不同基站/位置） | ~200-500 样本 | 跨场景测试 |
| Scenario 9 | 额外测试场景 | - | Challenge测试 |

每个样本包含：5 帧 RGB 图像（同步采集）、64 维 beam power 向量、GPS 坐标、最优 beam 索引标签。

### 4.2 评估协议

- **同场景评估**：场景内 70/15/15 划分，验证单场景拟合能力
- **跨场景评估**（核心）：训练 Scenario 1-4，验证 Scenario 5，测试 Scenario 6-8，评估泛化能力
- **指标**：Top-1 / Top-3 / Top-5 beam prediction accuracy

---

## 五、SOTA 对比

### 5.1 DeepSense 6G 视觉辅助波束预测 Leaderboard

| 方法 | 模态 | Scenario5 Top-1 | Scenario6 Top-1 | Scenario7 Top-1 | Scenario8 Top-1 | 备注 |
|------|------|----------------|----------------|----------------|----------------|------|
| **ASU Baseline (官方)** | Vision | 58.27% | 41.67% | 43.90% | 71.92% | 单帧 CNN，官方 2021 |
| **Vision-Position (WCNC'22)** | Vision+GPS | ~55% | - | - | - | Charan et al. |
| **BeamLLM** | Vision+LLM | 61.01% | - | - | - | LLM特征提取 |
| **Occlusion-Aware (2026)** | Vision+LiDAR+Radar+GPS | 50.92% | - | - | - | Scenario31，多模态 |
| **BEV-Fusion (2026)** | Vision+LiDAR+Radar+GPS | ~87% DBA* | - | - | - | BEV空间融合，需LiDAR |
| **Edge-Enhanced (2025)** | Vision+CSI | 88.7%* | - | - | - | IRJET，可信度待验证 |
| **CM-MAE v2（同场景）** | Vision+Beam+GPS | **62.67%** | - | - | - | 自监督预训练+Fine-tune |

\* 注：部分论文使用不同场景（如Scenario31/32/33/34）或不同指标（DBA而非Top-1），非完全公平对比。

### 5.2 我们的定位与优势

1. **唯一真正自监督预训练的方法**：现有 SOTA 均为端到端监督训练，我们的 CM-MAE 是首个将 MAE 范式引入 mmWave 跨模态预训练的工作，可利用海量无标注数据
2. **物理可解释的信道表示**：3D Beamspace + NMS 聚类提取传播路径，比黑盒 beam power 向量更具物理意义
3. **细粒度跨模态对应**：OT 损失建立像素区域-传播路径的对应关系，支持可解释性分析
4. **无需额外传感器**：仅需 RGB 相机 + GPS + 标准 beam power 向量，无需 LiDAR/Radar
5. **Beam Tracking 能力**：Seq2Seq 预测未来多帧波束，面向实际 V2I 移动场景

---

## 六、当前训练进展

### 6.1 预训练（ViT-Base，232M 参数，双卡 A100）

**关键发现**：之前的预训练因配置错误（模型仅 7.61M 参数、lr 过低）导致视觉特征未学到有效表示，linear probe 仅 8-15%。已修复配置后重新训练。

| 阶段 | 状态 | Best Val Loss | 关键指标 |
|------|------|--------------|---------|
| Stage A（重建） | ✅ 完成 100/100 ep | 0.0783 | l_rec_v: 0.026 (train) / 0.030 (val) |
| Stage B1（对比） | ✅ 完成 60/60 ep | 1.309 | l_ctr: 2.50 → **1.91**，对比损失有效下降 |
| Stage B2（几何掩码） | 🔄 进行中 ~38/60 ep | 1.309 | l_ctr 继续下降至 **1.91** |
| Stage C（OT对应） | ⏳ 待开始 | - | - |

**重建效果**：Stage A 后验证集图像重建可恢复场景整体结构和颜色，细节仍有提升空间（符合 MAE 特征，重建头非最终目标，核心在 encoder 特征质量）。

### 6.2 之前版本结果（基于旧预训练权重）

| 版本 | 任务 | 同场景 Top-1 (Sc1-4) | Val (Sc5) Top-1 | 跨场景 (Sc6-8) |
|------|------|---------------------|----------------|---------------|
| v1（CM-MAE 自监督） | 单帧 beam prediction | 62.67% | ~35% | ~5% |
| v2（时序+GPS） | 单帧 prediction | ~60% | 32.8% | ~5% |
| v3（Beam Tracking） | Seq2Seq 4帧预测 | 53-68% | 25.3% | 2.3% |

注：v3 跨场景差的原因已定位——**预训练权重质量不足**（旧预训练的 visual encoder 特征差），而非架构问题。ResNet18 ImageNet 特征在单帧任务上可达 55-63%，证明数据和框架本身没问题。**当前正在重新预训练 ViT-Base，预期将显著提升所有指标。**

---

## 七、可解释性分析

CM-MAE 框架天然支持可解释性分析：

1. **传播路径可视化**：NMS 聚类得到的路径簇可映射回波束空间，可视化哪些 AoA/AoD/时延路径被激活
2. **跨模态注意力热图**：Joint Decoder 中 cross-attention 权重可显示图像哪些区域影响了哪个路径簇的重建
3. **OT 对应矩阵**：Sinkhorn 传输矩阵揭示图像 patch 与 CSI 路径簇的软对应关系
4. **Beam 选择归因**：通过 attention rollout 追踪最终 beam 预测的视觉/信道/GPS 贡献比例

---

## 八、工程优化记录

| 问题 | 修复方案 | 效果 |
|------|---------|------|
| OT Loss 26s/iter | 完全向量化 Sinkhorn | → **1.3s/iter**（20×加速） |
| DDP AttributeError | model.module 访问原始模型 | 双卡训练正常 |
| CUDA 版本不匹配 | 切换 vision3d 环境 (PyTorch 2.1+cu121) | GPU 正常使用 |
| 预训练模型过小（7.61M） | 修复默认参数为 ViT-Base (d=768) | → 232M 参数 |
| l_rec_v 卡在 1.0 | LR 过低，恢复 1.5e-4 | 正常收敛至 0.026 |
| Beam Index Off-by-One | CSV 1-indexed → 0-indexed | 标签正确 |
| GPS 归一化错误 | BS-relative 坐标（米）替代硬编码 | 跨场景 GPS 有效 |
| GPS Gate 梯度消失 | 替换为零初始化残差头 | GPS 梯度通路正常 |
| 序列增强不一致 | 序列一致的亮度/对比度增强 | 时序特征稳定 |
| Disk 空间不足 | Checkpoints 迁移到 /data/ | 训练不中断 |

---

## 九、下一步计划

1. **完成 Stage C 预训练**（预计 1-2 小时）：OT 对应损失学习细粒度跨模态对齐
2. **Linear Probe 验证**：冻结 encoder 训练线性分类头，验证视觉特征质量（目标：单帧 Top-1 > 60%）
3. **接入 v3 Beam Tracking**：用新预训练权重重新 fine-tune，预期同场景 >70%，跨场景 >20%
4. **消融实验**：验证各组件（OT loss、GPS residual、时序 Transformer）的贡献
5. **可解释性可视化**：生成跨模态注意力热图和 OT 对应矩阵的定性结果
6. **与 SOTA 公平对比**：在统一协议下（Scenarios 1-4 train, 5 val, 6-8 test）对比现有方法

---

## 十、文件结构

```
/root/colum_comm/multi-modal_comm/
├── train_sota.py          # CM-MAE v1 预训练（ViT-Base，四阶段课程）
├── train_sota_v2.py       # v2 单帧预测 + 时序 + 跨场景
├── train_sota_v3.py       # v3 Beam Tracking (seq2seq) + GPS残差
├── eval_v3.py             # 跨场景评估脚本
├── data.py                # 数据预处理、Beamspace变换、NMS聚类
├── visualize_recon.py     # 重建可视化
├── debug_pretrained_encoder.py  # Linear probe调试
├── debug_resnet50_multiframe.py  # ResNet50+LSTM baseline
└── *.pdf                  # 相关论文参考
```

Checkpoints 和数据存储：
```
/data/cmmae/
├── deepsense6g/           # DeepSense 6G 数据集 (Scenarios 1-8)
├── ckpts_vitb_retrain/    # 正在训练的 ViT-Base 预训练权重
│   ├── best_A.pt          # Stage A best
│   ├── best_B1.pt         # Stage B1 best
│   └── best_B2.pt         # Stage B2 best (training)
├── ckpts_vitb_v2/         # v2 fine-tuned checkpoints
├── logs_retrain/          # 预训练日志
└── logs_retrain/recon_vis/# 重建可视化结果
```
