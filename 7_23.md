# CM-MAE 预实验训练进展报告

> **日期**: 2026-07-23
> **状态**: 🟢 训练进行中（Stage A: 35/50 epochs）
> **硬件**: NVIDIA A100-SXM4-40GB × 1
> **环境**: conda env `vision3d` (Python 3.10, PyTorch 2.1.2+cu121)

---

## 1. 实验配置

| 项目 | 值 |
|------|-----|
| 模型 | CM-MAE-Tiny (ViT-Tiny架构) |
| 参数量 | **7.61M** |
| d_model | 192 |
| Encoder layers | 6 (vis) + 6 (csi) |
| Decoder layers | 4 (joint) |
| 注意力头数 | 12 |
| 图像分辨率 | 224×224 (patch=16→196 patches) |
| CSI簇最大数 | 15 (3×3×3 cubes) |
| 数据集 | 5600训练 / 800验证 / 800测试（合成射线追踪） |
| Batch size | 64 |
| 优化器 | AdamW (β₁=0.9, β₂=0.95, wd=0.05) |
| 学习率 | 1.5e-4 (Stage A/B) → 5e-5 (Stage C), cosine+warmup(10ep) |
| GPU显存占用 | ~2.8 GB |
| 训练速度 | ~128 s/epoch |

### 四阶段课程设置

| 阶段 | Epochs | 核心损失 | Masking策略 |
|------|--------|----------|-------------|
| **A** (Warm-up) | 50 | L_rec_vis + β·L_rec_csi | 随机块掩码 (vis: 75%, csi: 50%) |
| **B1** (Global Align) | 60 | L_rec + λ₂·L_ctr (软对比) | 同A |
| **B2** (Geo-mask) | 60 | L_rec + λ₂·L_ctr | 几何引导跨模态掩码 |
| **C** (Correspondence) | 30 | L_rec + λ₂·L_ctr + λ₃·L_OT | 同B2 + OT路径-像素对齐 |

损失权重: β=1.0, λ₂=0.2, λ₃=0.1, OT正则ε=0.05, Sinkhorn迭代20次。

---

## 2. 训练环境

- **代码路径**: `/root/colum_comm/multi-modal_comm/`
- **Checkpoint路径**: `/data/cmmae/checkpoints/`
- **日志路径**: `/data/cmmae/logs/`
  - `train_output.log` — 终端输出
  - `train_log_seed0.csv` — epoch级CSV指标记录

启动命令：
```bash
cd /root/colum_comm/multi-modal_comm
PYTHONUNBUFFERED=1 /home/vipuser/miniconda3/envs/vision3d/bin/python -u train.py \
  --device cuda --batch_size 64 --num_workers 4 --seed 0
```

---

## 3. Stage A 训练进展（当前阶段）

### 3.1 Loss收敛曲线（Epoch 0 → 34）

| Epoch | lr | train_total | val_total | train_l_rec_v | val_l_rec_v | train_l_rec_c | val_l_rec_c |
|-------|-----|-------------|-----------|---------------|-------------|---------------|-------------|
| 0 | 1.5e-5 | **2.0586** | 2.0577 | 1.0441 | 1.0441 | 1.0145 | 1.0135 |
| 1 | 3.0e-5 | 1.9919 | 1.9825 | 1.0070 | 1.0009 | 0.9849 | 0.9816 |
| 2 | 4.5e-5 | 1.9791 | 1.9596 | 1.0002 | 1.0003 | 0.9789 | 0.9593 |
| 3 | 6.0e-5 | 1.9119 | 1.8842 | 1.0006 | 0.9998 | 0.9113 | 0.8844 |
| 5 | 9.0e-5 | 1.8624 | 1.8996 | 0.9990 | 0.9992 | 0.8634 | 0.9004 |
| 10 | 1.5e-4 | 1.8509 | 1.8447 | 0.9988 | 0.9988 | 0.8521 | 0.8459 |
| 15 | 1.4e-4 | 1.8260 | 1.8240 | 0.9987 | 0.9987 | 0.8273 | 0.8253 |
| 20 | 1.2e-4 | 1.8262 | 1.8250 | 0.9987 | 0.9987 | 0.8275 | 0.8263 |
| 25 | 9.8e-5 | 1.8223 | 1.8215 | 0.9987 | 0.9987 | 0.8235 | 0.8228 |
| 30 | 6.9e-5 | 1.8178 | 1.8127 | 0.9987 | 0.9987 | 0.8191 | 0.8140 |
| 34 | 4.6e-5 | **1.8167** | **1.8066** | 0.9987 | 0.9987 | 0.8180 | 0.8079 |

### 3.2 Loss下降趋势分析

**视觉重建损失 (l_rec_v): 1.044 → 0.999**
- 快速在前3个epoch从1.04降到1.00，之后趋于平稳（~0.999）
- 这是**符合预期**的：合成图像中大量patch是天空/地面等纯色背景（0.7灰），MSE天然较低，且背景区域即使mask也容易被周围颜色插值预测
- 说明图像编码器已学会基本的patch嵌入和重建

**CSI波束空间重建损失 (l_rec_c): 1.015 → 0.808**
- 从epoch 0到epoch 15持续快速下降（1.01 → 0.83），之后缓慢收敛
- 从epoch 27开始val loss达到0.798（最低），表明CSI簇重建学习效果**明显好于视觉**
- 波束空间立方体包含物理路径信息，NMS提取的cluster特征更有结构，模型容易学习

**总Loss (total): 2.06 → 1.81**
- 收敛趋势平滑，train/val gap很小（<0.01），无过拟合迹象
- Warmup前10个epoch lr线性上升，loss快速下降；之后cosine decay，loss缓慢收敛
- 预计Stage A最终val loss收敛在~1.80左右



---

## 4. 代码修改记录

本次正式训练启动前做了以下优化：

1. **数据预生成优化** ([data.py](file:///root/colum_comm/multi-modal_comm/data.py#L423-L458))：beamspace变换、NMS簇提取、归一化全部移到`_pre_generate()`阶段，训练时直接读取预计算结果，避免每batch重复计算

2. **OT损失向量化** ([losses.py](file:///root/colum_comm/multi-modal_comm/losses.py#L218-L249))：将Sinkhorn目标矩阵`A_rt`的Python for循环改为batch广播运算，Stage C单步时间从~26s降至~1.3s（**20x加速**）

3. **AUCpp指标向量化** ([losses.py](file:///root/colum_comm/multi-modal_comm/losses.py#L252-L285))：用广播比较替代四重for循环

4. **避免重复编码** ([train.py](file:///root/colum_comm/multi-modal_comm/train.py#L135-L149))：Stage C的OT head直接复用已编码token，不再重复跑encoder

5. **ViT CLS token修复** ([models.py](file:///root/colum_comm/multi-modal_comm/models.py#L145-L180))：CSI encoder设置`add_cls_token=False`（scale token已充当全局token），避免双重CLS导致shape错位

6. **CSV训练日志** ([train.py](file:///root/colum_comm/multi-modal_comm/train.py#L393-L410))：每epoch记录train/val各项指标到CSV，支持后续画收敛曲线

---

## 5. 观察与初步结论

1. CSI重建loss下降明显（0.20+），波束空间cluster表示有效
2. 视觉重建loss很快收敛，这是合成数据的特性（简单场景、纯色块多）
3. 无过拟合（train/val gap很小）
4. GPU利用率27%，显存2.8GB（A100 40GB充裕），数据加载不是瓶颈

### 后续阶段关注:
1. **视觉loss降幅有限**（0.04）：可能因为合成图像过于简单，后续需要更复杂纹理或真实数据
2. **Stage B1对比损失**加入后，l_ctr应从ln(2)=0.693开始下降（说明全局对齐在学习）
3. **Stage C OT损失**加入后，AUCpp应从0.5（随机）开始显著提升，这是跨模态细粒度对齐的核心指标
4. 物理正则项（TV平滑+波束空间稀疏）l_phys目前为0，后续若需要可开启

### ⏱️ 预计时间线
- Stage A剩余15 epochs: ~30分钟
- Stage B1 (60 epochs): ~140分钟
- Stage B2 (60 epochs): ~140分钟
- Stage C (30 epochs): ~70分钟
- **预计总训练时间: ~6-7小时**（预计今晚20:00左右完成）

---



---

